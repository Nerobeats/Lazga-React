import React from "react";
import Grid from "@material-ui/core/Grid";

// This component is not working yet! It's only here for testing purposes

const Favorites = () => {
  return (
    <Grid
      container
      style={{ justifyContent: "center", minHeight: "2160px" }}
    ></Grid>
  );
};

export default Favorites;
